########Usage#########
./Wrapper.sh <path to ilastik executable> <path to folder with input images> <Prefix for resulting files>
######################

NOTE: The inout images folder should have files ending with the suffix - "roi2". Change this in Wrapper.sh if needed. This is conducive to output from the previous step which separates ROI region from original image and performs dominant color analysis.